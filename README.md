# 🏆 [Squid Game] - پروژه کامل توسعه بازی Unity و Gamification

## 💡 هدف و کانسپت اصلی پروژه (Conceptual Goal)

این پروژه به عنوان یک PoC (Proof of Concept) با هدف ادغام مکانیک‌های بازی‌های Hyper-Casual با مفاهیم **Gamification** و **Play-to-Earn (P2E)** طراحی و توسعه داده شده است.

* **هدف اصلی:** ایجاد یک بستر سرگرم‌کننده و در دسترس برای کاربران برای تعامل مستمر با بازی و کسب **توکن‌های مجازی/ایردراپ** (در نسخه اولیه و برنامه‌ریزی شده).
* **وضعیت انتشار:** این بازی در بازار منتشر شده است.
* **نقش من:** توسعه‌دهنده اصلی - مسئول پیاده‌سازی مکانیک‌های بازی، ادغام تبلیغات، تنظیمات فنی و **پیاده‌سازی رابط کاربری**.

## 🛠️ ساختار فنی و تکنولوژی‌های کلیدی

| بخش | جزئیات فنی |
| :--- | :--- |
| **موتور بازی** | **Unity [نسخه واقعی را بنویسید، مثلاً 2022.3 LTS]** |
| **زبان و Scripting** | C# (با تمرکز بر کد تمیز و سازماندهی شده) |
| **Version Control** | Unity Version Controller |
| **ابزارهای درآمدزایی**| ادغام پلتفرم‌های تبلیغاتی (مانند Tapsell Plus) برای مدل درآمدزایی درون‌برنامه‌ای. |
| **سیستم‌های کلیدی**| **کنترلر کاراکتر و فیزیک (Character Controller & Physics)**، پیاده‌سازی سیستم مدیریت حالت بازی (Game State Machine)، پیاده‌سازی سیستم ذخیره‌سازی داده‌های محلی (Local Persistence). |

## 🕹️ مکانیک‌های پیاده‌سازی شده (Core Mechanics Implementation)

این پیاده‌سازی‌ها شامل پیاده‌سازی دقیق مکانیک‌های ۶ مرحله از فصل اول سریال، با رویکرد حل چالش‌های فنی زیر بودند:

### ۱. چراغ قرمز - چراغ سبز (Red Light - Green Light)
* **حرکت و توقف:** پیاده‌سازی قوانین فیزیکی و منطق State Machine برای مدیریت وضعیت حرکت و توقف کاراکترها.
* **هل دادن (Pushing):** پیاده‌سازی مکانیک‌های فیزیکی **هل دادن بازیکنان (NPC) توسط بازیکن اصلی** و مدیریت نیروهای متقابل (`Rigidbody2D` Interactions).
* **تشخیص خطا:** استفاده از سیستم‌های **Collider/Raycast** برای شناسایی دقیق حرکت غیرمجاز پس از اعلام توقف.

### ۲. شیرینی دالگونا (Dalgona)
* **برش (Cutting):** پیاده‌سازی مکانیک **Cutout / Slice** برای برش دقیق شیرینی به کمک کشیدن انگشت (Touch Input) و محاسبه آسیب (Damage) بر اساس نزدیکی مسیر برش به شکل اصلی.

### ۳. طناب‌کشی (Tug-of-War)
* **ورودی سریع (Rapid Input):** پیاده‌سازی سیستم ورودی مبتنی بر ضربات متوالی (Tap/Click Spamming) برای پر کردن نوار انرژی و محاسبه نیروی کششی تیم.
* **کنترل NPC:** مدیریت هوش مصنوعی NPCهای تیم برای افزایش خودکار نیروی تیم در صورت کاهش سرعت ضربات بازیکن.

### ۴. بازی تیله (Marbles)
* **انتخاب و استراتژی:** پیاده‌سازی سیستم انتخاب **گزینه‌های استراتژیک** و مدیریت ورودی‌های بازیکنان و NPCها.
* **سیستم شانس/مخفی:** پیاده‌سازی الگوریتم‌های **تصمیم‌گیری NPC** بر اساس شانس برای تعیین برنده هر دور.

### ۵. پل شیشه‌ای (Glass Bridge)
* **تشخیص پرش و سکو:** مدیریت پرش کاراکتر و **تشخیص سکوی فرود** (Landing Platform Detection) با استفاده از Raycasting.
* **سقوط و حذف:** پیاده‌سازی الگوریتم انتخاب سکوهای درست و پیاده‌سازی مکانیک **سقوط (Falling)** برای بازیکنان.

### ۶. بازی نهایی (Squid Game)
* **مکانیک هل دادن (PVP Push):** پیاده‌سازی مکانیک‌های برخورد فیزیکی (PVP Physics) برای **هل دادن بازیکنان (NPC)** و تلاش برای بیرون انداختن آن‌ها از زمین بازی.

---

## 📊 چالش‌های پروژه و درس‌های آموخته شده

توسعه این پروژه چالش‌هایی را در زمینه‌های مختلف ایجاد کرد که مهارت‌های من را افزایش داد:

* **چالش حقوقی (IP):** مواجهه با مشکلات کپی‌رایت برای استفاده از نام و برند "Squid Game" که منجر به تغییر رویکرد در طراحی مفهومی شد.
* **اقتصاد توکن:** چالش طراحی مدل اقتصادی پایدار و جلوگیری از تقلب در سیستم کسب توکن.
* **بهینه‌سازی:** بهینه‌سازی کد برای اجرا در اندروید با هدف حفظ نرخ فریم ثابت.

## 🔗 لینک‌های نمونه کار

| عنوان | لینک |
| :--- | :--- |
| **لینک بازی منتشر شده** | **[لینک مستقیم بازار/مایکت]** |
| **پورتفولیوی UI/UX مجزا** | https://dribbble.com/Aiden76 |
| **لینک پروفایل لینکدین**| www.linkedin.com/in/amirhosain-gazor |
